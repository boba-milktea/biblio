const Loading = () => {
  return (
    <div className="w-full bg-text opacity-25 h-screen z-50 flex items-center justify-center">
      <div className="mx-auto h-30 w-30 border-8 border-primary border-b-accent rounded-full animate-spin"></div>
    </div>
  );
};

export default Loading;
